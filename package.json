{
  "name": "memory-anchor-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix client\" \"npm run start --prefix server\"",
    "client": "npm run start --prefix client",
    "server": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "cors": "^2.8.5"
  }
}
